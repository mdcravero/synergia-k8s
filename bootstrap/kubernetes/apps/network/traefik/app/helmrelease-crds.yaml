---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: traefik-crds
  namespace: traefik
spec:
  chart:
    spec:
      chart: traefik-crds
      version: 1.1.0  # Versión de CRDs compatible con Traefik 34.x
      sourceRef:
        kind: HelmRepository
        name: traefik
        namespace: traefik
  interval: 1h
  install:
    createNamespace: true
  upgrade:
    remediation:
      retries: 3
