apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap-traefik.yaml
  - helm-source.yaml
  # - helmrelease-crds.yaml    <-- Uncomment if you want to manage CRDs with Helm
  - helm-release.yaml
