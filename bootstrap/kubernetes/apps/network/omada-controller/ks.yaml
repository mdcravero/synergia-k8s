apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app omada-controller
  namespace: flux-system
spec:
  targetNamespace: network
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  path: ./bootstrap/kubernetes/apps/network/omada-controller/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: omada-controller
  wait: false
  interval: 30m
  retryInterval: 1m
  timeout: 10m
  # postBuild:
  #   substitute:
  #     APP: *app
  #     VOLSYNC_CLAIM: omada-data-v2
  #     VOLSYNC_CAPACITY: 10Gi
  #     VOLSYNC_UID: "508"
  #     VOLSYNC_GID: "508"
