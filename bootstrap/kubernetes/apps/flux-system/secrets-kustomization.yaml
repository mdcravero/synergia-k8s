apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: external-secrets-authentik
  namespace: flux-system
spec:
  interval: 1m
  path: ./bootstrap/kubernetes/secrets
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: security
