apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: secret-mega
  namespace: backup-system
spec:
  target:
    name: secret-mega
    deletionPolicy: Delete
    template:
      type: Opaque
      data:
        mega-username: |-
          {{ .mega_username }}
        mega-password: |-
          {{ .mega_password }}

  data:
    - secretKey: mega_username
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: secret-mega
        property: mega-username
    - secretKey: mega_password
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: secret-mega
        property: mega-password