apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: secret-transmission-movies
  namespace: media
spec:
  target:
    name: secret-transmission-movies
    deletionPolicy: Delete
    template:
      type: Opaque
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"

  data:
    - secretKey: username
      sourceRef:
        storeRef:
          name: bitwarden-login
          kind: ClusterSecretStore # or SecretStore
      remoteRef:
        key: secret-transmission-movies
        property: username
    - secretKey: password
      sourceRef:
        storeRef:
          name: bitwarden-login
          kind: ClusterSecretStore # or SecretStore
      remoteRef:
        key: secret-transmission-movies
        property: password