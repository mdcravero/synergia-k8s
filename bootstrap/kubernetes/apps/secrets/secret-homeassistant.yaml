apiVersion: v1
kind: Secret
metadata:
  name: homeassistant-postgres-secret
  namespace: tools
stringData:
  # Acá armamos la URL de conexión que va a leer el configuration.yaml
  POSTGRES_URL: "postgresql://ha-user:TU_PASSWORD_SUPER_SECRETO@10.42.20.70:5432/ha-db"