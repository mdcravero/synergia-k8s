---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: homarr
  namespace: tools
spec:
  # dependsOn:
  #   - name: authelia
  #     namespace: security
  chart:
    spec:
      chart: homarr
      sourceRef:
        kind: HelmRepository
        name: homarr
        namespace: tools
      version: 8.20.0
  interval: 1m0s
  releaseName: homarr
  valuesFrom:
    - kind: ConfigMap
      name: homarr-values
      valuesKey: values.yaml 
