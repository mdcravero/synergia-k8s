apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: postgresql-cnpg

resources:
  # PostgreSQL Cluster (uses CRDs)
  - cluster-cnpg.yaml
  - service-cnpg.yaml

# Wait for operator to be ready
dependsOn:
  - name: cnpg-operator