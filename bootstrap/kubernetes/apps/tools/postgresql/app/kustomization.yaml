apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: postgresql-cnpg

resources:
  # Phase 1: CNPG Operator (creates CRDs)
  - helm-source-cnpg.yaml
  - helm-release-cnpg.yaml

  # Phase 2: PostgreSQL Cluster (uses CRDs)
  - cluster-cnpg.yaml
  - service-cnpg.yaml