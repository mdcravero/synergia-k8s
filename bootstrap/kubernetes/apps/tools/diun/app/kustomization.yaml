apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: diun
resources:
  - deployment.yaml
  - pvc-diun.yaml
  - clusterRole.yaml
  - clusterRoleBinding.yaml
  - serviceAccount.yaml

