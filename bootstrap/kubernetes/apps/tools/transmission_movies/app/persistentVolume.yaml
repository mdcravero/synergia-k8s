apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trans-config-movies
  namespace: media
spec:
  storageClassName: "nfs-client"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200M
