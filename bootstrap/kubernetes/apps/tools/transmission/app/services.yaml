apiVersion: v1
kind: Service
metadata:
  name: transmission
spec:
  type: LoadBalancer
  selector:
    app: transmission
  ports:
    - name: http
      protocol: TCP
      port: 9091
      targetPort: 9091
