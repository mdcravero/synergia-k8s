---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: flood
  namespace: tools
spec:
  chart:
    spec:
      chart: flood
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: flood
      version: 7.0.0
  interval: 2m0s
  releaseName: flood
  # valuesFrom:
  #   - kind: ConfigMap
  #     name: flood-values
  #     valuesKey: values.yaml
