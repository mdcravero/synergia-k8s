apiVersion: v1
kind: Service
metadata:
  name: deluge
  namespace: media
spec:
  type: LoadBalancer
  selector:
    app: deluge
  ports:
    - name: http
      protocol: TCP
      port: 8112
      targetPort: 8112
