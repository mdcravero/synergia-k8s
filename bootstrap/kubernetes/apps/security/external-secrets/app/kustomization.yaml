apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../crds/
  - helm-release.yaml

# Asegura que el HelmRelease se aplique solo después de los CRDs
# Esto solo funciona si usas Flux Kustomization para este directorio
