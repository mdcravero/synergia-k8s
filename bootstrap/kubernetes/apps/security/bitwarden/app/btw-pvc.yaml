apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: btw-config
  namespace: security
spec:
  storageClassName: "nfs-client"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
  volumeMode: Filesystem
  selector:
    matchLabels:
      app: bitwarden