apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: btw-down
  namespace: security
spec:
  storageClassName: "nfs-client"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi  