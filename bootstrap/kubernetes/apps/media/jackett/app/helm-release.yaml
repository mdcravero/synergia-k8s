---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: jackett
  namespace: media
spec:
  chart:
    spec:
      chart: jackett
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        name: jackett
      version: 1.31.1
  interval: 1m0s
  releaseName: jackett
  valuesFrom:
    - kind: ConfigMap
      name: jackett-values
      valuesKey: values.yaml
